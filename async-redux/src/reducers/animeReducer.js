export const initialState = {
    anime: null, 
    fetch: false, 
    error: ""
}

const reducer = (state=initialState, action) => { 
    switch(action.type){
        default: 
            return state;
    }
}

export default reducer;